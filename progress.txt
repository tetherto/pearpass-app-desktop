* **Iteration date/time:** 2026-03-04 00:00
* **Branch:** feat/fdroid-setup
* **Task(s) advanced:** Task 1 (Compatibility audit) — discovery
* **Changes made:**
  * Identified that this repository (`pearpass-app-desktop`) does not contain Android source/Gradle projects.
  * Identified that the Android app lives in `tetherto/pearpass-app-mobile` (Expo prebuild; `android/` dir generated).
  * Decided to execute the F-Droid readiness workstream against `pearpass-app-mobile`, while keeping this repo as the Ralph Loop log/driver.
* **Verification run:**
  * Searched repo for `AndroidManifest.xml`, `build.gradle`, and `androidx.credentials` usages (none found outside `node_modules`).
* **Results:**
  * Blocker to performing Android-specific tasks directly in this repo confirmed.
* **Open issues / blockers:**
  * Android project not present in `pearpass-app-desktop`; cannot add `fdroid` flavor, adjust version-check, or gate GMS here.
* **Next recommended step:**
  * Clone `tetherto/pearpass-app-mobile` locally and implement Tasks 1–12 there, starting with the compatibility audit ADR.

* **Iteration date/time:** 2026-03-04 00:25
* **Branch:** feat/fdroid-setup
* **Task(s) advanced:** Task 1 (Compatibility audit)
* **Changes made:**
  * Added `tetherto/pearpass-app-mobile` as a git submodule at `mobile/` to allow Android-focused work within this workspace.
  * Added the compatibility audit ADR in `mobile/docs/adr/ADR-0001-android-fdroid-compatibility-audit.md`.
* **Verification run:**
  * Confirmed audit findings by code search for `credentials-play-services-auth` and update redirect/version-check flows.
* **Results:**
  * Audit identifies the Play Services-backed credentials dependency injection and the Play Store-based version check/redirect as primary F-Droid blockers.
* **Open issues / blockers:**
  * None for Task 1; implementation tasks remain.
* **Next recommended step:**
  * Implement a dedicated `fdroid` Android build channel (via Expo config plugin + Gradle flavors) and verify `assembleFdroidRelease`.

* **Iteration date/time:** 2026-03-04 01:05
* **Branch:** feat/fdroid-setup
* **Task(s) advanced:** Task 2 (F-Droid flavor/channel)
* **Changes made:**
  * Added an Android build channel `fdroidRelease` in the mobile app via Expo config plugin (`plugins/withAppConfig`).
  * Added channel flags: `BuildConfig.DISTRIBUTION_CHANNEL` + `resValue("distribution_channel")` defaulting to `play`, overridden to `fdroid` for `fdroidRelease`.
  * Documented build commands and expected output path in `mobile/docs/fdroid/build.md`.
* **Verification run:**
  * `npm run build-plugins` (mobile) — OK.
  * `npm install` (mobile) — OK.
  * `npx expo prebuild --clean --platform android --no-install` — OK; confirmed `fdroidRelease` and channel fields present in `android/app/build.gradle`.
  * `./gradlew :app:assembleFdroidRelease -m` — FAIL (Android SDK not configured in this environment).
* **Results:**
  * F-Droid build channel is generated successfully during prebuild.
* **Open issues / blockers:**
  * Local Gradle verification requires Android SDK (`ANDROID_HOME` or `android/local.properties`).
* **Next recommended step:**
  * Make version-check behavior channel-aware so F-Droid builds do not scrape/redirect to Play Store.

* **Iteration date/time:** 2026-03-04 02:05
* **Branch:** feat/fdroid-setup
* **Task(s) advanced:** Task 3 (Version-check compatibility)
* **Changes made:**
  * Implemented Android distribution channel detection via a native module (`DistributionChannel`) generated during `expo prebuild`.
  * Updated `useVersionCheck()` to disable update checks for `fdroid` channel and avoid Google Play scraping/redirect.
  * Added unit test coverage for the F-Droid disable behavior.
  * Added `mobile/docs/fdroid/version-check.md` to document channel behavior.
* **Verification run:**
  * `npm test -- useVersionCheck.test.js` (mobile) — OK.
  * `npx expo prebuild --clean --platform android --no-install` — OK; confirmed `DistributionChannelModule.kt` + `DistributionChannelPackage` are generated and registered in `MainApplication.kt`.
* **Results:**
  * F-Droid builds no longer scrape Google Play or redirect to Play Store for update checks.
* **Open issues / blockers:**
  * Full Gradle build still requires Android SDK setup in this environment.
* **Next recommended step:**
  * Gate/exclude `androidx.credentials:credentials-play-services-auth` from the F-Droid build channel.

* **Iteration date/time:** 2026-03-04 02:30
* **Branch:** feat/fdroid-setup
* **Task(s) advanced:** Task 4 (GMS credentials gating — MVP)
* **Changes made:**
  * Updated the autofill Expo plugin so `androidx.credentials:credentials-play-services-auth` is only added for `debug` and `release` builds.
  * Confirmed generated `android/app/build.gradle` contains `debugImplementation`/`releaseImplementation` but not `fdroidReleaseImplementation` for the Play Services-backed artifact.
* **Verification run:**
  * `npx expo prebuild --clean --platform android --no-install` — OK.
  * Searched generated Gradle file for `credentials-play-services-auth` — present only as build-type scoped dependencies.
* **Results:**
  * F-Droid build channel no longer includes the Play Services-backed credentials auth dependency (MVP).
* **Open issues / blockers:**
  * Full Gradle build still requires Android SDK setup in this environment.
* **Next recommended step:**
  * Prepare F-Droid listing metadata/assets and draft `fdroiddata` recipe.

* **Iteration date/time:** 2026-03-04 03:00
* **Branch:** feat/fdroid-setup
* **Task(s) advanced:** Task 7 (Listing metadata/assets), Task 8 (fdroiddata recipe draft)
* **Changes made:**
  * Added `fastlane/metadata/android/en-US` in the mobile app with title/description and placeholder icon/screenshots.
  * Drafted a `fdroiddata` metadata file at `docs/fdroid/fdroiddata/com.pears.pass.yml`.
* **Verification run:**
  * Confirmed fastlane metadata files exist under `mobile/fastlane/metadata/android/en-US/`.
* **Results:**
  * Repository now includes an initial metadata baseline and an initial recipe draft.
* **Open issues / blockers:**
  * Screenshots are placeholders and should be replaced with real in-app screenshots before submission.
  * Recipe draft assumes an upstream tag that includes the F-Droid-specific patches.
* **Next recommended step:**
  * Prepare fdroidserver validation instructions and a non-GMS smoke-test checklist.

* **Iteration date/time:** 2026-03-04 03:30
* **Branch:** feat/fdroid-setup
* **Task(s) advanced:** Task 6 (Tags/versioning standardization)
* **Changes made:**
  * Added deterministic Android `versionCode` to the mobile app (`expo.android.versionCode`) using a `major*10000 + minor*100 + patch` scheme.
  * Updated fastlane changelog filename to match the computed `versionCode` (`10500.txt`).
  * Updated the draft `fdroiddata` recipe versionCode to `10500`.
  * Added `mobile/docs/release/fdroid-versioning.md` documenting tag/version conventions.
* **Verification run:**
  * `npx expo prebuild --clean --platform android --no-install` — OK; confirmed generated `android/app/build.gradle` has `versionCode 10500`.
* **Results:**
  * Versioning is now deterministic for F-Droid update checks.
* **Open issues / blockers:**
  * Upstream release automation (EAS remote app versioning) must be reconciled with this deterministic `versionCode` approach.
* **Next recommended step:**
  * Add fdroidserver validation instructions and a smoke-test checklist.

* **Iteration date/time:** 2026-03-04 03:50
* **Branch:** feat/fdroid-setup
* **Task(s) advanced:** Task 9 (fdroidserver validation), Task 10 (non-GMS smoke test), Task 11 (MR prep), Task 12 (internal docs)
* **Changes made:**
  * Added fdroidserver validation instructions: `docs/fdroid/fdroidserver-validation.md`.
  * Added non-GMS smoke-test checklist: `docs/fdroid/non-gms-smoke-test.md`.
  * Added F-Droid release checklist: `docs/fdroid/release-checklist.md`.
  * Added fdroiddata MR description template: `docs/fdroid/fdroiddata-mr-template.md`.
* **Verification run:**
  * Documentation-only iteration.
* **Results:**
  * Reproducible validation and submission guidance is now captured in-repo.
* **Open issues / blockers:**
  * fdroidserver build and non-GMS smoke test still require an external runner/device.
* **Next recommended step:**
  * Run the validation + smoke test on a real environment and capture results.

* **Iteration date/time:** 2026-03-04 04:10
* **Branch:** feat/fdroid-setup
* **Task(s) advanced:** Task 9 (fdroidserver validation)
* **Changes made:**
  * Ran initial `fdroidserver` validation inside `registry.gitlab.com/fdroid/fdroidserver:buildserver` container.
  * Updated validation doc to use `/home/vagrant/fdroidserver/fdroid` entrypoint.
* **Verification run:**
  * `/home/vagrant/fdroidserver/fdroid readmeta` — OK (warns about unsafe `config.yml` permissions).
  * `/home/vagrant/fdroidserver/fdroid lint com.pears.pass` — OK.
  * `/home/vagrant/fdroidserver/fdroid checkupdates --allow-dirty com.pears.pass` — FAIL.
* **Results:**
  * `checkupdates` fails because the recipe expects `versionCode 10500` but upstream tag `v1.5.0` still builds with `versionCode 1`.
* **Open issues / blockers:**
  * Need an upstream release/tag that includes deterministic `android.versionCode` changes before `checkupdates` can pass.
* **Next recommended step:**
  * Merge/tag the mobile F-Droid changes upstream, then rerun `checkupdates` + `fdroid build`.
